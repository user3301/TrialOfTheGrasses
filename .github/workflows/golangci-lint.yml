name: Github Actions
on: push

jobs:
  go-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions-contrib/golangci-lint@v1
        env:
          GO111MODULE: "on"
          GOFLAGS: "-mod=vendor"
